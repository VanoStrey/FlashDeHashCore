# 🔓 FlashDeHash

**FDH** — это высокоэффективный Java-инструмент для мгновенного восстановления исходных строк по их хэшу.  
Он не перебирает значения в реальном времени, а использует заранее отсортированные бинарные словари и двоичный поиск.

> 📦 Бинарный словарь весит в **2–3 раза меньше**, чем аналогичный `.txt` с теми же комбинациями  
> ⚡ Результат за ~1 мс даже на объёмах в десятки гигабайт. Работает оффлайн — без GPU и RAM-кеширования.

---

## 🧠 Основная идея

Вместо генерации строк на лету система:

1. Генерирует **все возможные 3-байтные значения**
2. Каждое значение **кодирует** в строку (по произвольному алфавиту)
3. **Хэширует** с помощью любого поддерживаемого алгоритма (`SHA-256`, `SHA-1`, `MD5`…)
4. Сохраняет в **чанки, отсортированные по хэшу**
5. Позволяет выполнять **мгновенный бинарный поиск**

---

## 📦 Архитектура

| Компонент                   | Описание                                                |
|-----------------------------|---------------------------------------------------------|
| `MasterChunkGenerator`      | Генерация исходного 3-байтного словаря                  |
| `ChunkValueEncoding`        | Преобразует значение в строку по произвольному алфавиту |
| `HashSortedChunkBuilder`    | Построение и сортировка чанков по хэшу                  |
| `ChunkBinaryFileAccessor`   | Побайтовый доступ к записи из чанка                     |
| `HashBinarySearch`          | Бинарный поиск по отсортированному хэш-массиву          |
| `Hasher` (интерфейс)        | Абстракция любого алгоритма хэширования                 |
| `CrackSHA256()`             | Многопоточный поиск по всем чанкам                      |
| `TelegramBot` (опционально) | Интерактивный Telegram-интерфейс                        |

---

## 🔧 Запуск и генерация

### 1. Генерация master-чана:

```bash
java coreChunk.MasterChunkGenerator
```

### 2. Сборка чанка:

```java
ChunkValueEncoding encoding = new ChunkValueEncoding("0123456789abcdef"); // любой алфавит
HashSortedChunkBuilder builder = new HashSortedChunkBuilder("master_chunk.bin", new SHA256Hash(), encoding); // любой хешь
builder.sortChunkToFile("chuncks",chunkIndex);
```

Чанк содержит 2²⁴ = 16 777 216 строк, отсортированных по хэшу.

---

## 🔍 Поиск

```java
String result = CrackSHA256("SHA256_HEX_HASH");
System.out.println("Результат: "+result);
```

- Каждый чанк обрабатывается отдельным потоком
- Применяется бинарный поиск без загрузки чанка в память
- Возвращается строка при первом совпадении

---

## 🔐 Поддержка хэшей

Поддерживаются любые алгоритмы через интерфейс `Hasher`:

```java
public interface Hasher {
    String getName();

    String getHash(String input);

    byte[] getBinHash(String input);

    byte[] hexToBytes(String hex);
}
```

Примеры: `SHA-256`, `SHA-1`, `MD5`, `BLAKE2b`, `Keccak`, `SM3`…

---

## 🔣 Гибкость алфавита

FlashDeHash поддерживает **любой алфавит** и **любой размер**:

- Символы задаются через `rangeChars` в `ChunkValueEncoding`
- Алфавит может содержать **латиницу, цифры, символы, эмодзи и т.д.**
- Это определяет систему счисления и длину результата

Примеры:

```java
new ChunkValueEncoding("0123456789"); // base-10 (только цифры)
new ChunkValueEncoding("abcdef");     // base-6
new ChunkValueEncoding("01");         // бинарные строки
new ChunkValueEncoding("ASCII set!@#..."); // 90+ символов
```

---

## 📊 Производительность

| Метрика                             | Значение             |
|-------------------------------------|----------------------|
| Время поиска SHA-256 в chunck_0.bin | **~0.8 мс**          |
| Записей в одном чанке               | **16 777 216 (2²⁴)** |
| Размер одного чанка                 | **48 МБ**            |
| Количество чанков                   | **356**              |
| Объём словаря                       | **~16.69 ГБ**        |
| Длина строки                        | до 5 знаков          |
| Кол-во символов в алфавите          | 94                   |
| Кол-во итераций поиска              | **~24**              |

> Поддерживаются любые фиксированные длины записи (в текущей версии — 3 байта)

---

## 📈 Нагрузочное тестирование

Проведён реальный бенчмарк на 1000 случайных SHA-256-хэшей (по строкам длиной от 1 до 5 символов). Тест охватывает
wall-time, CPU и RAM-потребление в боевом многопоточном режиме (CrackSHA256()):

```text
📊 Средняя нагрузка при расшифровке 1000 хэшей:
⏱ Среднее время подбора: 1,43 мс
📉 Среднее RAM на хеш: 0,75 МБ

🔧 CPU-время по потокам:
- [1] main: 671,88 мс
```

---

## 🖥️ Тестовая конфигурация

| Компонент          | Характеристика                            |
|--------------------|-------------------------------------------|
| Процессор (CPU)    | Intel Core i5-12400F (12th Gen)           |
| Оперативная память | **16 ГБ DDR5 @ 5600 MT/s (2× DIMM)**      |
| Накопитель         | Netac 1 ТБ NVMe SSD (PCIe 3.0 ×4)         |
| ОС                 | Windows 11 x64                            |
| JVM                | OpenJDK 21 (Corretto 21.0.7 LTS, 2025-04) |
| Логических ядер    | 12                                        |

> ⚡ Полный перебор 356 чанков (~16.69 ГБ) занимает менее 10 мс  
> 🧨 **Поиск работает исключительно на CPU — GPU не используется**

---

## 🔎 Как работает алгоритм

FlashDeHash реализует двухфазную стратегию: предварительную подготовку словаря и мгновенный поиск.

### 📥 1. Генерация словаря

- Для каждого `element` (3 байта) создаётся глобальный ключ:

  ```
  globalValue = element + chunkIndex × 2²⁴
  ```

  Результат представляет собой `byte[]` в **системе счисления 256**, где каждый байт — цифра.

- Этот массив переводится в строку через `ChunkValueEncoding`:

  ```
  systemBase = rangeChars.length()
  ```

  Таким образом, происходит переход из системы счисления 256 в любую кастомную — алфавит задаётся вручную.

- Полученная строка хэшируется с использованием заданной функции (`Hasher`)
- Все `element` сортируются по соответствующим хэшам
- В файл записываются только сами `element` (3 байта)

> Количество чанков не ограничено — система масштабируется линейно.

### 🔍 2. Поиск

- Пользовательский хэш конвертируется в `byte[]`
- Каждый чанк обрабатывается в своём потоке
- Используется бинарный поиск:

    - Получаем `element` по индексу
    - Вычисляем `globalValue = element + chunkIndex × 2²⁴`
    - Переводим в строку через `ChunkValueEncoding`
    - Хэшируем и сравниваем

- При совпадении возвращается исходная строка
- Поиск прекращается, как только хотя бы один поток возвращает результат

> Все операции происходят без загрузки всего файла в память, без GPU и без аллокаций.

---

## 🧪 Результаты производительности

```text
📎 Доступ к элементу: 3020.80 нс
🔤 Энкодинг: 6571.50 нс/запись
🔐 Хеширование: 29747.00 нс/запись
🧬 Хеш + энкодинг: 13561.50 нс/запись
🔎 Бинарный поиск: 0.69 мс, результат: 123
```

---

## 🛠️ TODO

- [ ] mmap-подключение файлов без аллокации
- [ ] Добавление REST API и Web-интерфейса
- [ ] Бенчмарк-режим с логами и экспортом
- [ ] CLI-выбор хэш-функции и алфавита

---

## 🤖 Telegram Bot

Поддерживает:

- Поиск по SHA-256
- Ответ пользователю в чате
- Параллельную обработку запросов

---

## 👤 Автор

FlashDeHash разработан Иваном (17 лет) — начинающим java разработчиком.  
Проект открыт и готов к масштабированию.

> *"Если хэш невозможно взломать — его просто никто ещё не отсортировал правильно."*
